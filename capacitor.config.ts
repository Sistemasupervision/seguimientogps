import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pnb.sistema.gps',
  appName: 'Sistema PNB GPS',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
